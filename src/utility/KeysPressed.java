package utility;

/*
 * Stores boolean variables which are modified when certain keys are pressed.
 * The values of these variables are used to respond to keyboard input.
 */
public class KeysPressed {
  public static boolean up, down, left, right;
}
